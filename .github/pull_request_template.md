<!-- Pull requests from outside Intuizi are not accepted; see the README. -->

What this changes, and why.

Prefix the commit subject so it lands in the right changelog group:
`feat:`, `fix:`, `deps:`, `docs:`, `test:`.

- [ ] `make test` and `make lint` pass
- [ ] Ran `scripts/live-test.sh` if this touches request building or output
